function aplicarEstilo() {
    var elemento = document.getElementById("chat-container");

    // Utiliza classList para agregar la clase 'estilo-desplegado'.
    elemento.style.removeProperty('display');
}